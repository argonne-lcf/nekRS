@kernel void boxInvMul(const dlong n, const scalar *x, @restrict const scalar *inv_mul, scalar *y) {
  for (dlong i = 0; i < n; i++; @tile(p_blockSize, @outer, @inner)) {
    y[i] = inv_mul[i] * x[i];
  }
}
