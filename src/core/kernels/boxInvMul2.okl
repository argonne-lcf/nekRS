@kernel void boxInvMul2(const dlong n, @restrict scalar *x, @restrict const scalar *inv_mul) {
  for (dlong i = 0; i < n; i++; @tile(p_blockSize, @outer, @inner)) {
    x[i] = inv_mul[i] * x[i];
  }
}
